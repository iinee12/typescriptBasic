{

    // Type Aliases

    type Text = string
    const name : Text = 'jinho'
    const address:Text = 'ssss'

    type Student = {
        name:string
        age:number
    }
    const student : Student = {
        name:'jinho',
        age:10
    }

    // String literal Type
    // 값 자체를 type으로 쓰는 것
    type JSON = 'json'
    let json :JSON = 'json'

}